# log-archieve-for-linux-machine
log archieve for linux machine using bash script
A simple command-line tool to archive logs by compressing them into .tar.gz files and storing them in a separate directory. This helps keep your system clean while preserving logs for future reference.
project url https://roadmap.sh/projects/log-archive-tool
